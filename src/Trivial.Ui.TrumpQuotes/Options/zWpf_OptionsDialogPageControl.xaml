<UserControl x:Class="Trivial.Ui.TrumpQuotes.OptionsDialogPageControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:SamplingMode="clr-namespace:System;assembly=mscorlib"
             xmlns:shell="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
             mc:Ignorable="d" 
             d:DesignHeight="70" d:DesignWidth="300"
             Background="{DynamicResource VsBrush.Window}"
             Foreground="{DynamicResource VsBrush.WindowText}" >

        <!--<Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <CheckBox Grid.Row="0" Name="CheckBoxIde" HorizontalAlignment="Left" Content="Show when Visual Studio IDE opens"/>
        <CheckBox Grid.Row="1" Name="CheckBoxSln" HorizontalAlignment="Left" Content="Show when opening a solution"/>
    </Grid>-->

    <StackPanel>
        <Slider Name="mySlider" Margin="10" Minimum="0" IsSnapToTickEnabled="True"/>
        <TextBlock Text="{Binding Path=Value, ElementName=mySlider, Converter={StaticResource dayOfWeekEnumToStringConverter}}" Margin="10"/>
    </StackPanel>

</UserControl>